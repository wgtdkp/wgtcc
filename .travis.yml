sudo: required
dist: Trusty
script:
  - sudo mkdir -p build
  - cd build
  - cmake ..
  - make
  - sudo make install
addons:
  apt:
    sources:
    - ubuntu-toolchain-r-test
    packages:
    - gcc-5
    - g++-5
    - cmake
